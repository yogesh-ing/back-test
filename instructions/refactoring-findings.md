# Refactoring Findings Tracker

Collects every missing point, deferred decision, edge case and follow-up
surfaced by the refactoring tickets (P1.1 → …) so they can be addressed
collectively later instead of being lost between tasks.

**Conventions**

- `Status`: `open` | `accepted` (deliberate V1 behavior, documented) | `done`.
- `Resolve in`: the ticket expected to pick the item up (or `—` if undecided).
- New findings: append a row, keep IDs unique; don't renumber.
- Ticket-interpretation notes: P2.1's `test_quick_screen_mode_still_works` names a "quick screen" mode that did not exist in the repo at the time (grep-verified) — it was implemented as a regression guard for the legacy vectorized `Backtester` path. P2.2 made `quick_screen` a REAL request mode (`"mode": "quick_screen"` on `/api/backtest/run` and per-slot on `/api/backtest/run-many`), so the naming now matches the code.
- P2.3 logging note: `run-many` worker jobs run in separate PROCESSES, so worker-side log records never reach the web process's log capture (and contextvars/request-id do not cross the boundary). The endpoint now re-emits the canonical per-slot `[result]` / `[slot N] failed` lines with the request id, and the worker returns its traceback inside the error payload (logged at DEBUG by the endpoint, popped before the JSON response so the UI still sees only `{"error": ...}`).
- P2.3 test-path note: the ticket names `tests/api/test_api_backtest_parallel.py`; created `tests/test_api_backtest_parallel.py` to match the repo's flat `test_api_*.py` convention (same call as P2.2).
- P3.1 notes: the ticket names `tests/brokers/test_base_contract.py`; updated the existing `tests/test_broker_base.py` instead (flat convention, same call). The ticket's ABC signatures reference `BrokerOrderId` / `BrokerOrder` / `MarginInfo`, so P3.1 also introduced those value types (broker-agnostic, in `brokers/base.py`, exported from `backtest.brokers`) plus an `ORDER_STATUSES` vocabulary — mStock's TypeA field mapping happens in P3.2 against these types.
- P3.2 notes: the ticket names `tests/brokers/test_mstock_order_mock.py`; created `tests/test_broker_mstock_order_mock.py` (flat convention, sibling of the existing `tests/test_broker_mstock.py`, same call). The ticket's pseudo endpoint map (`.../orders/{id}`, `.../calculate-margin`, `self._session.post(json=...)`) is illustrative — followed the archived reference the ticket itself cites: `modify/cancel → /openapi/typea/orders/regular/{id}`, margin → `POST /openapi/typea/margins/orders`; form packet for place/modify, JSON packet for margin; module-level `requests` (existing code style) with `Authorization: token <api_key>:<access_token>` per the reference's authenticated-request header. The order-book ROW schema is not documented in the archived reference, so `_order_from_row` maps best-effort by documented field names — verify against the live API before any real-account run.
- P3.3 notes: the ticket's pseudo signature `get_fill(order, next_bar) -> Fill` became `get_fill(order, market_data, quantity, rng) -> FillDecision`: (1) the seam sits in `execute()` where BOTH the tick and bar-clock paths converge (a bar is already converted to its open snapshot before `execute`); (2) `quantity` carries the executor's liquidity/participation cap; (3) `rng` is the run's single seeded generator passed in — a provider-owned RNG would desynchronise runs and break replay determinism (the executor's public `simulate_latency` still works, now delegating to the canonical module functions in `fill_providers.py`); (4) `FillDecision(fill, latency_ms)` wraps the fill because `ExecutionResult.latency_ms` must survive. `Fill` gained an additive `broker_order_id` field (default None; round-trips through to_dict/from_dict; DB-safe — `_insert_fill` copies explicit fields, no migration) plus `Fill.from_broker(raw, ...)` (mStock TypeA keys + generic aliases, `overrides` for brokers with thinner rows). `poll_fill(broker_order_id)` is a DUCK-TYPED live-broker requirement (a superset of the pinned 5-method P3.1 ABC — extending that ABC would break its pinned tests; a formal extension is P4/F-12 work).
- P3.4 notes: the polling loop (60s cadence, `_fetch_latest_bar`, scriptmaster symbol→security-token resolution, NSE market-hours gate) moved from the deleted live forward engine into `data/mstock_live_feed.py`: `iter_bars(symbol, max_bars, stop_check, market_gate, sleep)` generator with same-timestamp dedupe (a retried poll can't double-feed a bar) + `get_candles` for the DataSource contract (normalizes to an OHLCV frame). An injected `mstock_client` (`get_latest_bar`/`get_candles`) keeps it fully unit-testable; otherwise credentials resolve lazily at query time (`backtest.live.auth.get_session_token` + `MSTOCK_API_KEY`), so `SourceRegistry`'s no-arg construction stays safe. DROPPED with the deleted module (superseded by P1.x): the phantom-table persistence (`forward_test_state/trades/equity` had no DDL), the duplicate `PaperPortfolio`, and the background-thread engine. The engine's state FILE (`state/forward_test_state.json`, genuinely used, runtime-generated, untracked) was renamed to `state/forward_state.json` in `forward/engine.py` + `config/forward_testing.yaml` + `config/app.yaml` to purge the phantom vocabulary — old dev state files are orphaned (accepted, dev tool). `load_forward_test_results` (real analysis API) and the DB guide's `forward_test_*.dump` example remain — not table references. The feed's test module is named `tests/data/test_mstock_feed.py` (no "live") because CI runs `pytest -k "not live"` (tox.ini) and a "live"-named module would be silently deselected.
- P4.1 notes: the ticket's `PortfolioInstance.query` pseudocode had no backing model — the command-center instances live in the process-wide `PortfolioManager` (`StrategyRunner`s), so bucketing was added there: `RunnerConfig.mode` (paper|live, default paper) + `RunnerConfig.source` (P1.1 tag set: synthetic|replay|mstock, default synthetic), both exposed on `get_state()` rows; `PortfolioManager.list_instances(mode)` + `get_portfolio_summary(mode=None)` (scoped views aggregate over the bucket only; scoped `total_capital` = bucket allocations, scoped `peak_equity` = current bucket equity — bucket-level risk anchors deferred with F-12); `api/portfolio.py` public `list_instances(mode)` + `?mode=` on `/api/portfolio/summary` (400 on unknown mode) + spawn payload accepts `mode`/`source` (validated by RunnerConfig). Routes: `/portfolio` = landing with per-bucket summary cards (server-rendered from `list_instances`) over the full command center; `/portfolio/paper` + `/portfolio/live` = server-rendered bucket table (no-leak is HTML-level, testable without JS) + the command center included from the new `_portfolio_center.html` partial. The center JS (`portfolio.js`) reads `data-mode` from `#portfolio-page`: appends `?mode=` to the initial summary fetch, drops other-bucket rows from combined SSE snapshots, and stamps spawned instances with the page's bucket. Templates: `portfolio.html` rewritten as landing; `portfolio_paper.html`/`portfolio_live.html` new; command-center body extracted verbatim into `_portfolio_center.html` (block tags stripped — included files can't carry `{% block %}`). CI gotcha (second occurrence — standing rule): anything with the substring "live" in its test node id (module OR function name OR parametrize id) is deselected by tox's `-k "not live"`; the no-leak acceptance test is parametrized with ids `paper-bucket-page`/`broker-bucket-page` for that reason.
- P4.3 notes: LEAD DECISIONS (asked): canonical timeframe = the descriptive set `1min/5min/15min/1hour/4hour/1day/1week` (over compact and DB-native options); `config/marketdata.yaml` (no underscore, a third file the ticket didn't list) deleted too — its only reader was the deleted marketdata module. DELETED (replacements green first): `src/backtest/{marketdata,alerts,analysis,config_manager,dashboard}/`, the 7 test files that tested them (test_alert_manager, test_comparison, test_config_manager, test_dashboard, test_marketdata, test_marketdata_quality, test_timesync — 458 tests), and `config/{market_data,time_sync,marketdata}.yaml`. `forward/__init__.py` re-exported dashboard+alerts (verified no consumer) — removed before deletion. NOTE: this deletes `analysis/comparison.py` (`load_forward_test_results`) per the ticket — the P3.4 "keep it" call was about phantom-table naming, not the module's fate. DB-URL: `db/config.py::get_db_url(explicit=None)` is now the single authority (explicit > FORWARD_TEST_DB_URL > config/database.yaml profile); `data/db_source.py` and `api/data_manager.py` call it — the hard-coded postgres default and the bare `DATABASE_URL` alias are gone (no other consumers). Timeframes: `CANONICAL_TIMEFRAMES` + `MSTOCK_INTERVAL_MAP` (canonical→TypeA wire, the single copy shared by data_manager / mstock_live_feed / live MStockSource) live in `data/base.py`; API `_interval` (backtest + forward) is case-insensitive, permissive (fallback `1day` + warning, G6/G11); `RunnerConfig.timeframe` validated (default `1hour`); synthetic/csv `SUPPORTED_INTERVALS = ("1day",)`; CLI `--interval` defaults `1day`; spawn-UI options updated. Migration 003 (pg + sqlite + alembic + `tests/db/test_migrations_003.py`): the OLD CHECK must be dropped BEFORE the remaps (a remap like 'day'→'1day' violates it — caught by the test); the SQLite mirror folds the remap into the table-rebuild COPY via CASE (it can't drop the constraint in place); 60min→1hour, day→1day, week→1week remapped; 3min/30min/month rows deleted (re-ingestable cache). Acceptance verified: `grep -r "marketdata/\|live_engine\|config_manager\|dashboard/" src/` → 0 (two adapter docstrings mentioning the dashboard/compare/forward UI pages were reworded); DB-URL resolution in exactly one file (other mentions are comments documenting the chain); one timeframe vocab in config/DB/engine (old spellings survive only in tests that exercise the fallback/invalid-value paths: "1D"→1day warning, "3H" unsupported, "7min" CHECK-rejected). RESIDUAL (new finding): `live/market_data_handler.py` + `live/time_manager.py` still normalise legacy aliases ("day","1day","1d","d") as INPUT tolerance — a separate live subsystem not in the ticket's file list; fold into F-12. Suite drops 1997→1542 exactly by the 458 deleted orphan tests (plus 3 new migration tests); pre-existing lint debt (E501s, a few F401/F841 in test files) left as F-10 scope. The acceptance ("every instance card shows a badge") required wiring beyond the macro file: the server-rendered bucket tables (`portfolio_paper/live.html` — the old plain "Source" column became a "Mode/Source" badge column) and the JS command-center matrix (new `<th>Mode/Source</th>` in `_portfolio_center.html` + `badgeHtml()` in `portfolio.js` mirroring the macro — JS can't call Jinja macros). Display-label mapping is part of the contract: canonical P1.1 source tags shorten to the ticket's badge vocabulary (synthetic→SYNTH, mstock→MSTOCK, replay→REPLAY; unknown tags render uppercased as-is) — the raw tag stays canonical in data/DB, only the badge display shortens. CSS `.badge-paper`/`.badge-live` added to `static/css/app.css`; the badge's TEXT carries the value (accessibility acceptance) with colour/border only as supplement.

## Open findings

| ID | Raised in | Finding | Impact / why it matters | Status | Resolve in |
|---|---|---|---|---|---|
| F-01 | P1.3/P1.4 | `strategy_adapter.py` (~line 1159) still has a same-bar fill call site — the adapter's own execution path was not rewired to the single next-open fill path | Forward-engine adapter runs can still fill on the signal bar (look-ahead class of bug P1.3 fixed elsewhere) | done (see D-05) | — |
| F-02 | P1.4 | Walk-forward all-in sizing carries a 2% gap-safety haircut (`_all_in_size`) | Up to ~2% of each bucket idles in cash vs the old 100%-invested semantics; equity curves differ structurally from pre-P1.4 walk-forwards | accepted (trade-off for unfunded-fill safety) | revisit if exact capital-deployment parity is needed |
| F-03 | P1.4 | Walk-forward / live-loop executor uses the zero-cost `paper_free` profile; old walk-forward charged 8 bps per turnover | Cost-free results are not comparable to pre-P1.4 walk-forwards; realistic cost comparison needs a cost model | open | P3.x (fill providers / cost models) |
| F-04 | P1.4 | `run_live_papertrade` state-file format changed (account view: cash/position/entry_price/equity_history) | Old state files are not resumable; on resume an open position is folded into cash at the saved entry price (no PnL) | accepted (dev tool; format break documented) | — |
| F-05 | P1.4 | Package exports `Portfolio` (engine) and `SimulatedBroker` removed from `backtest.forward` (replaced by `StrategyPortfolio` / `PaperBroker`) | No in-repo importers (grep-verified); out-of-repo scripts could break | accepted | announce in changelog (P4.x) |
| F-06 | P1.5 | PaperRunner transition corner case: a **partial-fill** close plus a re-entry pair can over-sell when bar volume is thin → `DuplicatePositionError`, caught by the run-loop backstop; run continues but a position stub can linger until the next 1→0 | Only on thin-volume data + exact re-entry timing; V1 paper runs use `max_participation="1"` so it's rare | accepted (backstop keeps the run alive) | P3.x (partial-fill policy) |
| F-07 | P1.4 | The unfunded-fill backstop (extreme open gap) can leave a *phantom fill* on the order's audit trail (order has a fill, no portfolio movement) | Cosmetic audit-trail blemish only; cash/equity/positions unaffected | accepted | P3.x |
| F-08 | P1.5 | Ticket P1.5 names `BacktestDriver` (a P2.1 deliverable) before P2.1 exists | Equivalency was proven against the vectorized `Backtester` on gapless bars; the driver leg was a skipped placeholder sharing the harness | done (P2.1) | — |
| F-09 | P1.4 | Stale docs: `docs/FORWARD-TESTING.md` and `docs/PORTFOLIO-CENTER.md` reference the deleted modules (`forward.paper`, `forward.broker`, `forward.runner`, `forward.order_ledger`, `forward.portfolio`, `SimulatedBroker`) | Docs mislead readers to deleted code | open | P4.x (docs cleanup) |
| F-10 | P1.1–P1.4 | Lint baseline debt (pre-existing flake8 findings in `cli.py`, `api/portfolio.py`, `portfolio_manager.py`, …) — verified unchanged vs HEAD | Not introduced by the refactor; ticket scope says don't fix unrelated debt | open | P4.x (cleanup) |
| F-11 | P2.x (forward) | P2.1 junior note: if `BacktestDriver` copy-pastes more than ~10 lines of the `PaperRunner` loop, factor a shared `run_engine_loop(source, strategy, executor, portfolio, order_queue)` into `simulator/engine_loop.py` | Reuse > duplicate; keeps backtest and forward on literally one loop | done (P2.1) | — |
| F-12 | P3.x (forward) | Wire live execution end-to-end | Broker ABC + mStock order HTTP done (P3.1/P3.2, mocked); fill seam done (P3.3 — `BrokerFillProvider` ready, needs a broker with `poll_fill`); live feed DONE (P3.4 — `MStockLiveFeed.iter_bars`/`get_candles`); bucket UI DONE (P4.1 — mode/source tags, scoped pages, spawn accepts mode); REMAINING: execute mode='live' runners through `BrokerFillProvider` + `MStockLiveFeed` (forward-engine wiring), `poll_fill` in the formal ABC, bucket-level risk anchors, order-book row mapping verified against the real API, legacy timeframe-alias tolerance in `live/market_data_handler.py` + `live/time_manager.py` normalised to the P4.3 canonical set | open | P4.x |
| F-13 | P4.x (forward) | UI/cleanup pass | — | open | P4.x |
| F-14 | P2.1 | `BacktestDriver` carried a LOCAL mirror of `paper_runner.SOURCE_TAGS` (importing from `backtest.forward` would close a package cycle) | Two tag maps that can drift (new source class must update both) — resolved in D-06; one canonical map in `data/source_tags.py` | done (D-06) | — |
| F-15 | P2.2 | Canonical `/api/backtest/run` results changed structurally vs pre-P2.2: next-bar-open fills vs prev-close, `_all_in_size` 2% haircut (F-02), zero-cost `paper_free` profile vs the vectorized 8 bps — e.g. DEMO 2021–24 sma 10/30: 15 vs 16 trades | UI shape is identical (same `BacktestAdapter`), but absolute numbers differ from anything run before P2.2; old numbers remain available via `mode='quick_screen'`; payload now carries `config.engine` (`backtest_driver` \| `quick_screen`) as an additive marker | accepted (simulator-exact path is the canonical one, per ticket) | document in the P4.x changelog/docs pass (F-09) |
| F-16 | P2.3 | `run-many` worker processes rebuild their data source BY NAME (`build_source(source_name)`); a source object never crosses the process boundary (not picklable — e.g. `DbSource` engines, HTTP sessions) | Any future source with per-instance state (caches, cursors, non-deterministic per-instance seeding) would re-derive data per worker and change run-many semantics | accepted (all current sources are plain-constructor; `SyntheticSource` is deterministic per symbol) | revisit whenever a new DataSource class is added (P3.x / P4.x) |
| F-17 | F-01 | Signal decisions moved from PORTFOLIO-position-based to TRANSITION-based (`_last_target`), with a flip-aware bookkeeping fix. Verified equivalence: with fills succeeding, old decision(t) = old `decide(position_after_fill(t-1), target(t))` with same-bar fills ⇒ `position_after_fill(t-1) == target(t-1)` (clipped), which is exactly the new transition decision. `tests/test_strategy_adapter.py::test_transition_decisions_match_old_position_based` pins IDENTICAL action sequences vs a reference old-state machine for: persistent long, 1-bar spikes, re-entry, persistent short, short spikes, direct flips long↔short (incl. flip-then-flat and flip back-and-forth), with and without `allow_short`. Flip bookkeeping (done in F-01): on a direct flip the close-only action must set `_last_target = ZERO` so the opposite side re-opens the NEXT bar (raw `_last_target = target` made the open short/long never fire) | One REAL divergence remains, and it is the reviewer-accepted one: **identical repeated signals no longer re-fire.** Old logic re-fired BUY/SELL on every bar while no fill landed (dry-run, order rejected at creation, no executor — position never advanced). New logic fires once per 0→1 / 1→0 transition and then HOLDs. Mitigation: unfilled orders are still retried by the executor queue (`step()` re-attempts working orders at each next bar's open), so only CREATION-time rejections lose their retry — the same semantics as the canonical `run_engine_loop`. `test_persistent_signal_fires_once_not_every_bar` pins it | accepted (canonical-engine semantics; documented) | — |
| F-18 | (#ticket #9) | `tox.ini` still gates the suite with `pytest -k "not live"`, which deselects `tests/forward/test_live_engine.py` (module name contains "live") — the T8 deterministic live hard-gate (broker fills, resume, no-downgrade) never runs under tox, so live-path coverage is silently missing from the CI gate while the canonical `pytest tests/` run covers it | The `-k "not live"` filter was a broad-brush guard for network-dependent tests; today the deterministic live engine tests are fake-broker-only and the only real-network test (`test_mstock_live_integration`) is already `skipif`-gated. Fix: rename the T8 module (e.g. `test_real_engine.py`) or split the filter (`-k "not live_integration"`). Same cleanup bucket as the benchmark/env friction — verified-closure note (ticket #9) assigns it to the T11 housekeeping ticket. **CLOSED in ticket #10 (D-13):** the filter is now `-k "not live_integration"` (tox.ini) — the T8 `tests/forward/test_live_engine.py` hard-gate (7 tests, fake-broker) runs under tox; only the opt-in, env-gated `tests/test_mstock_live_integration.py` stays deselected. Ticket #10's checklist folds F-18 in here, superseding the T11 assignment | done (ticket #10, D-13) | Ticket #10 (folded in) |

## Pitfalls (known traps, for future tasks)

| ID | Pitfall | Notes |
|---|---|---|
| K-01 | Sizing entries off `current_cash` collapses when a close is still in flight (cash ≈ 0, equity = full value) | Any all-in sizer must size off `calculate_total_equity()`, not cash (this cost one debug round in P1.5; `_funded_all_in` in the equivalency test is the reference) |
| K-02 | The simulator `Order` class and the ledger `Order` dataclass share a name | In `paper_runner.py` the simulator one is imported as `SimOrder`; keep it that way |
| K-03 | `executor.execute()` raising `InsufficientFundsError` on an unfunded fill is a **tested contract** (`test_simulator_execution.py::test_portfolio_limits_still_apply`) | Don't soften it in the simulator; handle it at the runner level (haircut sizing + caught backstop) |
| K-04 | Gapless-data precondition: prev-close (backtester) == next-open (paper runner) only when `open[t] == close[t-1]` | Any equivalency test on real (gapped) data must state the fill-anchor difference explicitly |
| K-05 | Sandbox resets wipe `~/.venv` repeatedly | Rebuild: `python3 -m venv ~/.venv && ~/.venv/bin/pip install -q -r requirements.txt`; run tests with `PYTHONPATH=src` |

## Done

| ID | Raised in | Finding | Resolution |
|---|---|---|---|
| D-01 | P1.5 | 1-bar signal spike (long for exactly one bar) was missed: the entry fills at the bar's open *after* the exit signal's decision ran, so the close was skipped and the position ran away to the next 1→0 | Fixed in `PaperRunner.run()`: queue the matching close behind the in-flight entry (`tests/simulator/test_engine_equivalency.py` locks it in) |
| D-02 | P1.5 | 1-bar re-entry (flat→long on the bar right after a close) was missed: the decision ran before the close executed, saw the position still open, and blocked the re-entry | Fixed in `PaperRunner.run()`: re-entry is allowed while a close of the symbol is in flight (the buy arms one bar behind the close) |
| D-03 | P2.1 | The P1.5 equivalency test's `BacktestDriver` leg was a skipped placeholder (F-08) | `BacktestDriver` (`engine/backtest_driver.py`) implemented over the SHARED `run_engine_loop` (`simulator/engine_loop.py`); the placeholder was replaced with a real driver leg asserting an EXACT P&L match vs PaperRunner (one loop, two entry points) plus the COST_TOLERANCE property vs the vectorized Backtester |
| D-04 | P2.1 | PaperRunner's ~130-line loop was duplicated into BacktestDriver (junior-note factor trigger) | Factored into `simulator/engine_loop.run_engine_loop` (Reuse > duplicate). `Bar`/`OrderQueue`/`to_python_scalar` moved to the simulator layer to respect the layering rule (simulator must not import forward); `paper_runner` re-exports `OrderQueue` for import compatibility |
| D-05 | F-01 | `strategy_adapter.py` still filled on the signal bar (`on_bar_close` → `execute_signals` → `executor.execute` at ~L1159), and `ForwardTestingEngine` used that path — the one remaining same-bar fill leak (F-01) | Adapter is now signal/order-creation ONLY: `execute_signals` replaced by `create_orders` (no `executor.execute`), `executor` constructor param + `on_order_filled` removed, signal decisions are TRANSITION-based (previous target → new target, not portfolio position — the portfolio isn't updated until the next bar, so position-based decisions would re-fire orders). `ForwardTestingEngine` drives the canonical bar clock in both `run_loop` (new-bar dedupe via `_last_bar_ts`, `_new_bars`, `_to_executor_bar`) and `_run_backtest_mode`: signal → Order → `executor.submit` → `executor.step(next bar)` fills at the next bar's OPEN. **Forward-engine P&L numbers changed** (same class of change as F-15) — noted in `docs/FORWARD-TESTING.md` change log. Acceptance: `tests/test_forward_engine.py::test_forward_engine_fills_at_next_open_not_signal_bar` + adapter-level `tests/test_strategy_adapter.py::test_orders_fill_at_next_open_not_signal_bar`; P1.5 equivalency and the full suite (1571 passed) green |
| D-06 | F-14 | `SOURCE_TAGS` duplicated verbatim in `forward/paper_runner.py` and (as `_SOURCE_TAGS`) in `engine/backtest_driver.py` — two sources of truth that could drift | Moved to `data/source_tags.py` (`backtest.data.SOURCE_TAGS`): canonical `dict[type, str]` `{SyntheticSource: synthetic, DbSource: replay, MStockLiveFeed: mstock}` (copied from the real shape — values are lowercase, key is `MStockLiveFeed` not `MStockSource`), plus `source_tag_for(source, default="synthetic")` helper unifying the three `.get(type(source), "synthetic")` fallbacks. Both callers import it (no cycle — module imports only `backtest.data.*` siblings; `db/config.py` is stdlib-only); `paper_runner` and `backtest.forward` keep re-exporting `SOURCE_TAGS` for import compatibility; `backtest.data` re-exports it too. `_SOURCE_TAGS`, the local dicts and the three now-unused source-class imports were deleted. Verified: exactly one definition in `src/`; `SOURCE_TAGS is` identical object across `backtest.data` / `backtest.forward.paper_runner` / `backtest.forward`; full suite green |
| D-07 | DB-T1 (/ticket #4) | Verified: migration 002 (mode/source on `portfolios`) already shipped with the P1.1 refactor — `db/migrations/002_add_mode_source.sql` + `.sqlite.sql` mirror + alembic revision `002` (chained: 001→002→003), `schema_migrations` ledger insert, `tests/db/test_migrations_002.py` (fresh + legacy backfill + CHECK rejection + ORM parity + PG-file convention checks) | Gap fixed: `db/models.py::Portfolio.mode/source` had only `server_default`; added the Python-level `default="paper"` / `default="synthetic"` so ORM-only inserts are covered too (ticket DB-T1 explicitly requires both; DB default unchanged). Full suite 1582 passed / 4 skipped. Docs (`docs/DATABASE.md`) already mention the 002 columns | — |
| D-08 | F-04 (/ticket #5) | Forward state FILE (`state/forward_state.json`) had no classification: `state_version` absent and no `mode`/`source`, so state alone couldn't bucket a run (portfolio DB had them since migration 002; the file lagged) | Format **v2**: `STATE_VERSION = 2` + `engine_id` (`portfolios.portfolio_id`) + `mode` (paper|live, validated against the ORM `PortfolioMode` — `backtest` data mode maps to the `paper` bucket, simulated fills) + `source` (canonical `SOURCE_TAG_VALUES` from `data/source_tags.py`; no re-declared strings — added `SOURCE_TAG_VALUES` frozenset as the single allowed-values set and reused it in `paper_runner`). Values come from the engine's ACTUAL `config.data` via `_classify()` (duck-typed, no hardcoded default) on save AND load. **Old-file handling (decision): legacy v1 files (no `state_version`) load, are normalized in memory (invalid values warn + engine-derived fallback) and rewritten to v2 on the next save — load itself stays read-only; a file from a NEWER version is refused (warn + no load).** `ForwardTestingConfig.to_dict()` unchanged (state carries the classification; config remains the source). Docs: `docs/FORWARD-TESTING.md` State Persistence. Tests: `test_forward_engine.py` — parametrized config→payload (paper/synthetic, paper/mstock, live/mstock, backtest→paper), legacy v1 load+migrate-on-save, future-version refusal, invalid-classification fallback. DB-T4 folded remnant re-verified: `forward_test_*` in `src/ db/ tests/ config/` → zero hits (only archive docs/instruction history) | — |
| D-09 | (#ticket #6, backtest unification) | **AUDIT — backtest construction sites**: (1) `engine/backtest_driver.py` — `BacktestDriver`, the documented canonical fill-exact engine (P2.1); (2) `api/backtest.py::_run_driver` — the ONLY canonical-driver bootstrap, but it imported `_FrameSource`/`_all_in_size`/`free_executor` from `forward/paper_runner.py` (engine → forward layering violation by proxy) and duplicated: `_interval` (clone of `api/forward.py::_interval`), `_trim_to_range` (clone of `forward.py::_trim_to_range`), warmup computation (×2 in api/backtest + clone in `forward.py::_load_candles`), quick-screen run+trim call (×2 in api/backtest + ×1 in forward /start), no-signal warning + metrics stamping (clones of `runner.run_on_candles`); (3) `runner.py::run_backtest/run_on_candles/compare_strategies` — legacy vectorized `Backtester` path (CLI `run`/`compare` + API `mode='quick_screen'`; NOT dead — CLI + F-15 pinned); (4) `forward/paper_runner.py` — `_FrameSource`/`_all_in_size`/`free_executor` defined HERE but used by `BacktestDriver` callers; `run_walkforward` + resume path re-implement the same bucket bootstrap | **Canonical entry = `backtest/engine/backtest_runner.py`**: single `run_backtest` (fill-exact driver → `BacktestResult`; moved verbatim from api `_run_driver`), `run_quick_screen` (vectorized + trim), `trim_to_range`, `resolve_interval`, `resolve_warmup_start` (api clones removed; api/backtest + api/forward now IMPORT; forward /start uses `run_quick_screen`). **Shared primitives moved to neutral homes** (no forward→engine dependency): `FrameSource` → `data/frame_source.py`; `free_executor` + `PAPER_FREE_PROFILE` → `simulator/execution.py` + `simulator/fees.py`; `all_in_size` → `simulator/position_sizing.py`; `paper_runner` keeps `_FrameSource`/`_all_in_size`/`free_executor` as compat aliases (tests still import from there — object identity pinned by `tests/engine/test_backtest_runner.py`). **Divergent paths explicit, not forced**: `runner.py` stays the LEGACY vectorized runner (its `run_backtest` renamed `run_on_source`; CLI updated) — switching CLI to the driver would change numbers (F-15), forbidden by "no behavior change"; the vectorized path's contract is documented in `runner.py` + `engine/backtest_runner.run_quick_screen`. API Flask view renamed `run_backtest_endpoint` (URLs/endpoint-usage unchanged; no `url_for` refs) so `def run_backtest` exists exactly once. Verified: checklist grep → `def run_backtest` ×1, `class BacktestDriver` ×1, zero `_trim_to_range`/`_interval`/`_FrameSource`/`_all_in_size`/`free_executor` defs outside canonical homes; audit line + notes also in `docs/ARCHITECTURE.md` + `docs/BACKTEST-ENGINE.md`. Tests: new `tests/engine/test_backtest_runner.py` (5: canonical==driver P&L, canonical==PaperRunner P&L, quick_screen==vectorized+trim, API delegates/identity + clones gone, shared-helper identity); full suite 1594 passed / 4 skipped (1589 + 5) | — |
| D-10 | (#ticket #7, forward state follow-through) | **STATE SAVE/LOAD CYCLE — full map.** SAVE: `StateManager.save_state` (`forward/engine.py`) called from (a) `stop()` (always), (b) `run_loop` + `_run_backtest_mode` when `should_save` (interval>0); payload = state_version/engine_id/mode/source/portfolio.to_dict (T5) + adapter.get_state + performance + config + loop_count. LOAD: `initialize_system` reads it TWICE (portfolio restore, then adapter restore); `state_version`>current refused; v1/v2 normalized. **Restored today (before T7):** portfolio id/name/cash/status/mode/source/limits/**positions**/**closed_positions**/equity_history, realized_pnl, commission; adapter symbols/_bars/_last_target/_indicators/_state. **NOT restored (gaps):** portfolio ORDER ledger (pending+filled), executor bar-clock queue (pending/armed — an order armed at teardown was LOST, missed its fill), `_loop_count` (initialize_system reset it to 0, and run_loop reset it again), `_last_bar_ts` (live-loop dedupe → the last bar could be re-processed), and `_run_backtest_mode` had NO processed-bar offset → a restore replayed the whole history against a portfolio that already held the positions (double exposure; only masked by F-17 transition suppression once the target was past) | **Fixed — v3 (STATE_VERSION 2→3, additive; v1/v2 still load with a documented caveat):** `Portfolio.to_dict/from_dict` now round-trip the full order ledger (`orders` key; `Order` already had exact JSON round-trip, its docstring explicitly names the Step 20 snapshot); new `OrderExecutor.get_state/restore_state` (pending FIFO + `armed`/`queued` id sets — an armed order fills on the FIRST bar after restore, preserving exact timing; `restore_state(orders=portfolio.pending_orders)` shares the same order objects so `sync_orders` sees the executor's transitions); `save_state` adds `executor` + `engine_runtime` {loop_count, last_bar_ts (canonical `_ts_key` iso strings), processed_bars per symbol}; `initialize_system` restores all three at the end (after the `_loop_count = 0` reset) with warn-and-continue guards; `_new_bars` dedupe compares canonical keys (restored ts vs live ts always equal-shaped); `_run_backtest_mode` replays `candles.iloc[processed:]` and tracks `_processed_bars[symbol]` per bar — a restored backtest resumes at the next unprocessed bar; `run_loop` resumes via restored `_last_bar_ts`. **Canonical-path honesty:** resuming re-enters the SAME loop methods a fresh run uses (`start()` → `_run_backtest_mode`/`run_loop`; no restore branch), and the executor arm/fill semantics are the P1.3 canonical bar clock — pinned by the fill-at-first-resumed-bar test. **Divergence noted, not blocked (T8-relevant):** v1/v2 files can't carry in-flight execution state — documented in STATE_VERSION + docs/FORWARD-TESTING.md; fresh runs are unaffected. Tests: `tests/test_forward_state_roundtrip.py` (7): resume==never-stopped at teardown-after-2-bars (armed order) and after-6-bars (settled), resumed pending order fills at bar 2 with identical price/size to never-stopped, v3 payload fields, v2 file loads + converges, portfolio order-ledger JSON round-trip, executor armed-timing round-trip. Suite: 1601 passed / 4 skipped (1594 baseline + 7 new round-trip tests, no regressions) | — |
| D-11 | (#ticket #8, live end-to-end) | **LIVE PATH MAP — live-only responsibilities vs what existed.** (1) Live data feed: `data/mstock_live_feed.py::MStockLiveFeed` (iter_bars, real-time, dedupe) + `live/market_data_handler.py::MarketDataHandler` (MStockBrokerFeed over MStockSource; `get_latest_data()` streaming) — EXISTED, and `engine.run_loop` already polls it; NO test had ever run the live loop headlessly (run_loop was entirely untested). (2) Live order execution: `simulator/fill_providers.py::BrokerFillProvider` (place_order+poll_fill, REAL fill) EXISTED, but the engine NEVER injected it — `initialize_system` always built a `SimulatedFillProvider` executor, so a `mode='live'` run would silently paper-trade; and `brokers/mstock.py::MStockBroker` lacked `poll_fill`, so the real broker couldn't drive the seam (the F-12 "needs a broker with poll_fill" item). (3) Live classification: `_classify` → live/mstock for state (T5) EXISTED, but the engine's FRESH portfolio was created with `Portfolio()` defaults (`mode="paper"/source="synthetic"`) → DB row for a fresh live run would be paper/synthetic — a clean silent classification downgrade. **GAPS FIXED.** — engine: `ForwardTestingEngine(..., broker=None)` (duck-typed; default `MStockBroker()` — construction safe, no auth, `_require_session` fails cleanly) + `initialize_system` now wires `BrokerFillProvider(broker)` when `_classify()[0]=='live'` (simulated provider otherwise) + fresh portfolio created with `mode/source` from `_classify` + **no-downgrade guard** after restore/provision: config live & portfolio paper → upgrade live/warn; config paper & portfolio live → reclassify paper/warn (config authoritative; accounting untouched). **REAL BUG FOUND & FIXED (BrokerFillProvider):** it called `place_order` on EVERY attempt — a still-working order was re-placed at the venue on every bar (double-placement). Fixed: place once, stamp `order.broker_order_id` (already round-tripped by T7 state v3), retries poll the SAME broker order; restored orders never re-place. **New `MStockBroker.poll_fill`:** `GET /openapi/typea/orders` row lookup by order_id → normalized `Fill.from_broker` row (filled_quantity + average_price; partial never over-states) or None while open/unknown; `_fetch_order_rows()` factored out of `get_order_book`. **Tests (hard gate — deterministic live simulator, no real venue/feed):** `tests/forward/test_live_engine.py` (7) — fresh live start (broker fill not simulated, portfolio/state live), streaming dedupe (repeated bars, one 0→1 transition, one order), order stays working until broker reports (retries, one placement), DB `portfolios.mode='live'`+`source='mstock'` + fills row from REAL fill price, resumed live (v3 restore → same run_loop, armed order placed ONCE + fills first resumed bar, live classification preserved), no-resurrect-as-paper guard, paper-never-claims-live guard; `tests/simulator/test_fill_providers.py` +1 (retry polls same broker order, never re-places); `tests/test_broker_mstock_order_mock.py` +4 (poll_fill open→None, complete→normalized row, partial→filled qty, unknown→None). Divergence noted: teardown can catch an order armed but not yet placed (venue first contacted at first FILL attempt) — the resumed run places it once, which is correct (nothing lost, nothing doubled). Full suite: 1613 passed / 4 skipped (1601 baseline + 12 new T8 tests: 7 live-engine + 1 fill-provider + 4 poll_fill) | — |
| D-12 | (#ticket #9, bucket risk limits) | **AUDIT — current risk/sizing logic (pre-ticket, GLOBAL):** (1) `ForwardTestingConfig.risk` — one global `RiskConfig` (max_position_size=10000, max_positions=5, drawdown/daily) from `config/forward_testing.yaml`; (2) `PortfolioLimits` on a FRESH engine portfolio = `allow_short` + `max_open_positions = config.portfolio.max_open_positions or config.risk.max_positions` — value/pct/gross/min-trade caps ALWAYS None (untouched by config); (3) `PositionSizer` built from `config.sizing` with NO `constraints` → `SizingConstraints()` all-None → sizing ran with method params only and NEVER capped by any risk limit; (4) `RiskManager` (Step 15 real) built from global `config.risk` but **NEVER CONSULTED** — `validate_order` had no caller anywhere in engine/executor/adapter, so the step-15 risk manager was decorative in the forward path; only `Portfolio.can_open_position` (limits) actually rejected anything; (5) canonical `backtest_runner.run_backtest` + `PaperRunner` size via `all_in_size` (98% all-in) with `free_executor`/`PAPER_FREE_PROFILE`, unconditional — the same global knob for every bucket. NOTHING was keyed on `_classify()`'s (mode, source) **BUCKET DEFINED (canonical map, `simulator/bucket_risk.py`):** `paper` = free play (all exposure caps None, min-trade None, leverage 1, all sources accepted — matches the PAPER_FREE_PROFILE intent) vs `live` = real capital (max_position_value 10000, max_position_pct 0.10, max_gross_exposure_pct 0.50, max_open_positions 5, min_trade_value 1000, leverage 1) + **source gate**: live refuses `synthetic`/`replay` (fake-data-trading-real-money hazard) — paper accepts every SOURCE_TAG. `mode` keys caps; `source` gates what can be traded. **WIRING (resolve where `_classify` resolves, T3 pattern — no re-declared literals):** `initialize_system` resolves `self._bucket_key/_bucket_limits = resolve_bucket_risk(mode, source, overrides=self.config.risk.buckets)` right after `_classify`, REFUSING (ValidationError) unknown buckets/sources before any trading; fresh/restored/provided portfolios ALL re-key `limits` from the bucket (`bucket.to_portfolio_limits`) with the old explicit config position cap as fallback when the bucket leaves it open; `PositionSizer` gets `constraints=bucket.to_sizing_constraints()` (size reaching the executor is already bucket-limited); `RealRiskManager` config built from `bucket.to_risk_config(...)` (drawdown/daily stay config-level); **live-only pre-trade check** in `_submit_orders` (`_bucket_risk_allows`) — every real-fill order passes the bucket risk manager before `executor.submit` (paper = free play, no order-time check); `backtest_runner.run_backtest` resolves the paper bucket for its portfolio (permissive → historical P&L unchanged, pinned by T6). **RISK TEETH on the T8 guard:** if the classification guard had to CHANGE the restored portfolio's mode/source, `bucket.check_exposure(portfolio)` runs on the OPEN book — a violation RAISES ("RISK REFUSAL — mis-classified … run"), instead of silently trading at the wrong size (the empty-book T8 guard cases still upgrade normally). **TESTS (14 new, all deterministic):** `tests/test_simulator_bucket_risk.py` (7: defaults differ paper|live, source gate, unknown bucket/field raises, explicit override merge + None-disable + tight-paper, limit validation, adapter mapping to sizing/portfolio/risk config, stub-book exposure check incl. value/pct/gross/open) + `tests/forward/test_bucket_risk.py` (7: identical inputs → paper 476 vs live 95 (10k/10% caps), override wins both directions (tight paper 95, loose live 476), live/synthetic+replay refused at engine, mis-classified 476-lot paper book → live config RAISES, restore re-keys limits+sizer from bucket, risk check called for real-fills only (recorder), run_backtest resolves paper bucket (spy)). T8 fixture: `_live_engine` now passes an explicit permissive `risk.buckets.live` override (comment: pins live PLUMBING, not caps; boundary covered by the T9 file) — its 476-lot expectations are unchanged. Suite: 1627 passed / 4 skipped (1613 + 14). **NEW FINDING (F-18):** tox.ini still runs `pytest -k "not live"`, which deselects `tests/forward/test_live_engine.py` (module name contains "live") — the T8 deterministic live hard-gate never runs under tox; the T9 module follows the standing rule (safe name, no "live" in node ids). T8 module-name fix is the cheap resolution | — |
| D-13 | (#ticket #10, UI restructure) | **UI AUDIT — every UI→backend reference mapped.** (1) `forward.html#dataMode` conflated the taxonomy axes: it sent `synthetic|live` as **mode** — a label the backend had no source axis to check, so a "live" run could silently paper-fill (the exact trap T8/T9 closed server-side). (2) `/api/forward/start` accepted no `source` (server picked data; the user's choice was invisible). (3) The forward page showed NO classification: no run-mode/source selects, no risk implication, no live-refusal surfacing, and copy said "live paper trading" (an oxymoron the page silently paper-filled). (4) Portfolio spawn modal `#spawn-target-mode` was mislabelled — it sets the target TYPE (promote/backtest/forward), not the risk mode; `#spawn-target-type` is the canonical name. (5) Currency labels: `backtest.html`/`compare.html` showed `$` next to Capital (project currency is INR; the template already injects `currency_symbol`). (6) Already canonical (audited, no change): `_macros.html` badge, `portfolio.js` `SOURCE_LABELS` (display-only shortening; raw tag stays canonical in data/DB — JS can't call Jinja macros, P4.3-documented divergence), `api/portfolio.py` runner-create validation, `RunnerConfig.mode/source`, `promoteToForward`/`forwardPrefill` (carry strategy/symbol config only; classification is chosen AT START, never pre-filled). **FIXES — mode & source are first-class UI dimensions, never implicit.** Templates (`forward.html`, `_portfolio_center.html`) render the selects from `bucket_modes`/`source_tags` injected by `app.context_processor` (`inject_globals`) — values derive from canonical `BUCKET_RISK_LIMITS` / `SOURCE_TAG_VALUES` (import, don't duplicate); JS (`forward.js`, `portfolio.js`) only READS the controls; grep-verified `dataMode`/`spawn-target-mode`/old "live paper trading" copy: zero hits in `src/`. Mode: `runMode` (paper|live) with T9 risk copy in `taxonomyHint` (live = tight caps 10%/10k/5-pos + refuses synthetic/replay + requires mStock; paper = free-play caps, accepts all sources). Source: `dataSource` (synthetic|replay|mstock) all first-class, with the source-trust note for paper+mstock ("LIVE DATA, PAPER RISK"). **Server refuses silently-wrong starts:** `/api/forward/start` resolves `(mode, source)` via `_resolve_classification` → canonical `resolve_bucket_risk` (unknown bucket/source → 400 with backend's own message; live+synthetic/replay refused BEFORE anything starts); auth guard now applies to LIVE only (paper needs no session); `mode=live` (even authenticated) → 400 `live_execution_not_wired` with honest copy — never paper-fills a live label; start/status/summary echo `mode`/`source` and snapshot `config` carries them. **Resume affordance (T7-aware):** `resumeBanner` + `resumedSessionId` + `resumeBtn`/`freshStartBtn` — refresh re-attach shows the running session's id and Resumes vs Start Fresh is a visible choice; stop clears banner/buckets; re-attach restores buckets from status config. New canonical map `APP_SOURCE_TAGS` + `app_source_tag()` (`data/source_tags.py`): web config names (`synthetic|csv|mstock|db`) → taxonomy tags (`csv→synthetic, db→replay`) so an omitted `source` is never hardcoded. `/api/config` now returns `taxonomy.modes`/`taxonomy.sources`. Portfolio center: spawn modal exposes `spawn-mode`/`spawn-source` + risk hint. CLI: `papertrade` keeps its T8 `--mode walkforward|live` vocabulary (run shape, not classification — changing it would break the T8 contract); `--source` was already validated by `build_source` (unknown → error) — added `data_source_tag=<tag>` print via the canonical `app_source_tag()` so the classification is visible at start. **WIRING PROOF.** `tests/test_web_taxonomy.py` (4, headless): selects render canonical mode/source values with risk copy, resume affordance present + honest copy, spawn modal exposes target-type/mode/source + hint, `/api/config` taxonomy, old `dataMode` absent. `tests/test_api_forward.py` (+6, one renamed): live refusal after auth, live refuses synthetic/replay, unknown mode/source 400, paper accepts all three sources with start/status echoing them, omitted source defaults through the app-config map, expiring-soon counts as authenticated then still 400-not-wired, auth-before-not-wired order. `tests/js/test_forward_auth_gate.mjs` (11→12): taxonomy-driven gate (paper default starts without auth; live+unauthenticated/expired disables + "Connect mStock" button; live+authenticated enables), selection payload asserts `mode=paper/source=mstock` (replaces the stale `dataMode` case), resume banner + fresh-start test (harness gained the shared `Money` stub); `test_forward_widgets.mjs` 7 unchanged. Pre-existing tests encoded the OLD "default start requires auth" contract and were updated to the T10 split (paper ≠ auth-gated, live is): `test_security_verification`, `test_broker_expiry::test_expired_blocks_forward_start` (+paper-still-starts assertion), `test_logging_config::test_forward_lifecycle_is_logged` (mode/source taxonomy + live refusal log), `tests/manual/test_auth_flow_integration` (3 spots), `test_broker_ui` count 11→12. **F-18 folded in:** tox.ini `-k "not live"` → `-k "not live_integration"` — the old filter silently deselected 39 tests including T8's `tests/forward/test_live_engine.py` (now 7 passed under tox); only the opt-in env-gated live-integration module stays deselected. **Suite:** `pytest tests/ -q -k "not live_integration"` → **1636 passed / 3 skipped / 1 deselected** (1627/4 baseline + 9 new T10 tests: 5 api_forward net + 4 web_taxonomy). The `benchmarks/` dir is NOT part of the tox `tests/` gate and stays outside this number (its `pytest-benchmark` fixture comes from the tox `benchmark` env, not requirements.txt, and its load tests have host-dependent timing thresholds — pre-existing, untouched by T10; flagged, not fixed, to avoid scope creep). **T10 session-2 addendum (verification pass, 2026-09-01):** re-audited every UI→backend reference against the code above — the checklist held, and the pass closed three residual drifts the session-1 audit missed, all in the ticket's "no re-declared backend constants / hardcoded labels" scope: (1) `web/app.py::_portfolio_buckets()` and `templates/portfolio.html` each re-declared the bucket vocabulary (`("paper", "live")` / `["paper", "live"]`) — now driven by the canonical `BUCKET_RISK_LIMITS` map (the landing page iterates the injected `bucket_modes`, same source as every mode select); pinned by `test_landing_page_bucket_cards_driven_by_canonical_map` (landing cards == canonical map keys exactly). (2) The portfolio-side mirror of audit finding (5): server-rendered money labels hard-coded `₹` in `portfolio.html` (4×), `_portfolio_center.html` (2×), `portfolio_paper.html`/`portfolio_live.html` (2× each) and `forward.html` (initial equity) — on a `--currency USD` deployment these pages printed ₹ while Forward/Backtest printed $; all now render `{{ currency_symbol }}`, and `deep_dive.js`'s 3 hard-coded ₹/`en-IN` formatters now use the config-driven `Money` component (single formatter, G3 contract). Pinned by `test_portfolio_and_forward_pages_render_configured_currency_symbol` (USD deployment → `$` present, no `₹` in any server-rendered portfolio/forward HTML; verified to FAIL pre-fix). (3) Tracker hygiene: the F-18 row still read `open | Ticket #11 (housekeeping)` although its fix shipped in T10 (this ticket's checklist folds F-18 in, superseding T9's T11 assignment) — row now closed with the D-13 cross-reference. JS behaviour unchanged (harnesses stub `body.dataset` per the G3 contract; `deep_dive.js` is not under a JS harness). Suite: 1636 → 1638 passed / 3 skipped / 1 deselected (the two new landing/currency pins). | — |
