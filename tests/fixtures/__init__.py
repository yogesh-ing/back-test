"""Test fixtures and data generators for forward testing simulator (Step 24)."""

# Fixtures provide reusable market data samples, corrupted data, etc.
# Used by unit and integration tests

# Example fixtures are inline in test files, but this package centralizes them
# as per Step 24 spec:
#   tests/fixtures/
#   └── market_data_samples.py
